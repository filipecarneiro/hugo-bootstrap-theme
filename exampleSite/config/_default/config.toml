baseURL = 'https://filipecarneiro.github.io/hugo-bootstrap-theme/'
title = "Hugo Bootstrap Theme"
author = "Filipe Carneiro"
copyright = "Open-source <a class=\"text-muted text-decoration-none\" href=\"https://github.com/filipecarneiro/hugo-bootstrap-theme/blob/main/LICENSE\">MIT</a> Licensed."

timeZone = 'Europe/London'

# Multilingual
defaultContentLanguage = "en"
defaultContentLanguageInSubdir = false

theme = "hugo-bootstrap-theme"
themesdir = "../.."

disableHugoGeneratorInject = true
enableRobotsTXT = true
#enableGitInfo = true
enableEmoji = true
#sectionPagesMenu = "main"
#googleAnalytics = "UA-1511XXXX-X"

[frontmatter]
  date = ["date", "publishDate", "lastmod"]
  # :git before :fileModTime. A CI checkout rewrites every file's modification
  # time, so :fileModTime would stamp every page with the build time - making
  # sitemap <lastmod>, JSON-LD dateModified and article:modified_time all claim
  # the whole site changed on every deploy. :git needs --enableGitInfo, which
  # the build workflow passes; "date" is the fallback when neither applies.
  lastmod = ["lastmod", ":git", "date"]

[taxonomies]
  tag = "tags"
  category = "categories"
  series = 'series'
  author = "authors"

[sitemap]
  changefreq = "weekly"
  filename = "sitemap.xml"
  priority = 0.5

[permalinks]
#  post = "/post/:title/"

[module]
  [module.hugoVersion]
    extended = true
    min = "0.99.1"
    max = ""
  [[module.mounts]]
    source = "assets"
    target = "assets"
  [[module.mounts]]
    source = "static"
    target = "static"
